from pathlib import Path

DATA_PATH = Path(__file__).parents[2] / "data" # Define the path to the data directory by navigating two levels up from the current file's location and appending "data" to it